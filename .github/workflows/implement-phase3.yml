name: Implement Phase 3

on:
  push:
    branches:
      - phase-3-research-xray

permissions:
  contents: write

jobs:
  implement:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Apply Phase 3 implementation
        run: python scripts/implement_phase3.py
      - name: Format changed sources
        run: npx prettier --write src/integrity/xray.js src/integrity/provenance.js src/integrity/ResearchIntegrityPanel.jsx src/components/CodeMirror.jsx tests/research-integrity.spec.ts
      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Build Phase 3 Research X-Ray"
          git push origin HEAD:phase-3-research-xray
